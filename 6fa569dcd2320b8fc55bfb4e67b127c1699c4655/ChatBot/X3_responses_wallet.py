# The file has two functions for the Wallet Requests mode:
    # Checks if the request is wallet
        # Returns True or False
    # Creates a Response
        # Returns a response_text

## ---------------------------------------------------------- ##
def check_if_wallet_request(request_content):
    return False


## ---------------------------------------------------------- ##
def wallet_response(request_content):
    return ["Wallet responses are not currently supported."]
